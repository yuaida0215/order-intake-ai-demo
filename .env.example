APP_PASSWORD=          # ログイン用の共有パスワード（商談相手に伝える）
AUTH_SECRET=           # Cookie署名用の秘密鍵。`openssl rand -hex 32` で生成。公開しない
CHATWORK_API_TOKEN=    # Chatwork APIトークン（マイチャット等の管理画面から発行）
CHATWORK_ROOM_ID=      # 取り込み対象のChatworkルームID
ANTHROPIC_API_KEY=     # Claude API キー（受注情報抽出に使用）
